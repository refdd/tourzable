import ContactUsForm from "@/components/contactUs/ContactUsForm";
import FormCustomizeTour from "@/components/customizeTour/FormCustomizeTour";
import FromTourGuides from "@/components/hleper/FromTourGuides";
import FromTourOders from "@/components/hleper/FromTourOders";
import MainHeaderList from "@/components/list/MainHeaderList";
import Footer from "@/components/mainSections/Footer";
import Subscribe from "@/components/mainSections/Subscribe";
import WhyChooseUs from "@/components/mainSections/WhyChooseUs";
import HeaderSections from "@/components/parts/HeaderSections";
import { baseUrl, fetchApi } from "@/utils/ferchApi";
import Head from "next/head";
import React from "react";

function TourOders({ cities }) {
  // console.log(cities);
  return (
    <>
      <Head>
        <title>{t("common:home.Customize_trip")}</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <MainHeaderList title={t("common:home.Customize_trip")} />
      <HeaderSections
        titel={t("common:home.Customize_trip")}
        desc="Lorem ipsum dolor sit amet, consectetur adipiscing elit."
      />
      {/* <FormCustomizeTour cities={cities} /> */}
      <FromTourOders cities={cities} />
      <WhyChooseUs />
      <Subscribe />
      <Footer />
    </>
  );
}

export default TourOders;
export async function getStaticProps({ locale }) {
  const cities = await fetchApi(`${baseUrl}/cities?locale=${locale}`);

  return {
    props: {
      cities: cities.data,
    },
    revalidate: 10,
  };
}
